import wollok.game.*
import personajes.*
import niveles.*

program Game {
	game.title("Granja lamari")
	game.height(config.height())
	game.width(config.width())
	game.boardGround("ground.png")

	nivel1.iniciar()

	game.showAttributes(hector) //Debug
	
	game.start()
}