import wollok.game.*
import personajes.*
import niveles.*

program Game {
	game.title("Granja de lamari")
	game.height(10)
	game.width(10)
	game.boardGround("ground.png")

	nivel1.iniciar()

	game.showAttributes(hector) //Debug

	game.start()
}